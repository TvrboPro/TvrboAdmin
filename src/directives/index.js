module.exports = function(app){

	// app.directive('purchasesMarkProcessing', require('./purchases-mark-processing'));
	// app.directive('purchasesMarkDone', require('./purchases-mark-done'));
	// app.directive('purchasesGenerateWaybills', require('./purchases-generate-waybills'));

	// app.directive('purchaseMarkProcessing', require('./purchase-mark-processing'));
	// app.directive('purchaseMarkDone', require('./purchase-mark-done'));

};
